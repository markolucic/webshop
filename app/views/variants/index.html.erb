<div class="row">
	<h3 style="text-align:center;">Products variants</h3>
</div><br>
<div class="row">
	<% @product.variants.each do |v| %>
		<div class="col-md-3 col-xs-6 col-sm-4">
			<%= image_tag(v.product.img.url(:medium)) %><br>
			<%= v.product.name %><br>
			<%= v.size.size %><br>
			<%= v.color.name %><br>
			<%= v.quantity %>KOM<br>
			<hr>
		</div>
	<% end %>
</div>