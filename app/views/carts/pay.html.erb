<%= form_tag cart_path %>
	$<%= @current_user.total_price*1000 %>

	<script src="https://checkout.stripe.com/v2/checkout.js" class="stripe-button"
			data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
			data-description="ABHShoes product"
			data-amount="<%= @current_user.total_price*1000 %>"></script>
			
<% end %>